{
  "name": "mobile",
  "version": "1.0.0",
  "main": "index.ts",
  "dependencies": {
    "@react-native-async-storage/async-storage": "2.2.0",
    "@react-navigation/native": "^7.3.16",
    "@react-navigation/native-stack": "^7.18.8",
    "expo": "^54.0.0",
    "expo-status-bar": "~3.0.9",
    "react": "19.1.0",
    "react-native": "0.81.5",
    "react-native-safe-area-context": "~5.6.0",
    "react-native-screens": "~4.16.0"
  },
  "devDependencies": {
    "@types/react": "~19.1.10",
    "typescript": "~5.9.2"
  },
  "scripts": {
    "start": "expo start",
    "start:local": "cp .env.local .env && expo start",
    "start:dev": "cp .env.dev .env && expo start",
    "android": "expo start --android",
    "android:local": "cp .env.local .env && expo start --android",
    "android:dev": "cp .env.dev .env && expo start --android",
    "ios": "expo start --ios",
    "ios:local": "cp .env.local .env && expo start --ios",
    "ios:dev": "cp .env.dev .env && expo start --ios",
    "web": "expo start --web",
    "web:local": "cp .env.local .env && expo start --web",
    "web:dev": "cp .env.dev .env && expo start --web"
  },
  "private": true
}
