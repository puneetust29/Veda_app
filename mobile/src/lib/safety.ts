// Safety & Sharing Framework — interfaces only.
// No messaging integrations are implemented in this version.
// These types define the data contract for future safety features.

export type {
  TrustedContact,
  ArrivalShareEvent,
  SafetyCheckEvent,
  SafetyWorkflowDefinition,
} from '../types';
