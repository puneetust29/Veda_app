# Expo HAS CHANGED

This app is on **Expo SDK 54** (`expo@~54.0.x`, React Native 0.81, React 19.1).
Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before
writing any code.

Do not follow SDK 57 docs or examples. The app was deliberately downgraded from
57 to 54 in commit 354245b; APIs that exist in 57 may be absent or different here.
Confirm the installed version before trusting any snippet:

```bash
npx expo-doctor          # must report packages matching the installed SDK
node -p "require('expo/package.json').version"
```
